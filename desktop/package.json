{
  "name": "headroom-desktop",
  "productName": "Headroom",
  "version": "3.2.0",
  "description": "Headroom — budgets, assets, investments and loan modeling",
  "author": "Morten Nordbye",
  "license": "Apache-2.0",
  "private": true,
  "main": "main.js",
  "scripts": {
    "postinstall": "electron-builder install-app-deps",
    "sync": "node scripts/sync.js",
    "start": "npm run sync && electron .",
    "dist": "npm run sync && electron-builder",
    "verify": "node scripts/verify.js"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "express": "^5.2.1",
    "express-rate-limit": "^7.5.1",
    "helmet": "^8.3.0"
  },
  "devDependencies": {
    "electron": "^43.3.0",
    "electron-builder": "^26.15.3"
  }
}
